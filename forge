#!/usr/bin/env bash
set -euo pipefail

DIR="$(cd "$(dirname "$0")" && pwd)"

if [[ "$(uname -s)" == "Darwin" ]]; then
    brew bundle --file="$DIR/Brewfile"
    stow -d "$DIR" -t "$HOME" macos
fi

stow -d "$DIR" -t "$HOME" dotfiles
