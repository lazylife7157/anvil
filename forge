#!/usr/bin/env bash
set -euo pipefail

DIR="$(cd "$(dirname "$0")" && pwd)"

if [[ "$(uname -s)" == "Darwin" ]]; then
    brew bundle --file="$DIR/Brewfile"
    stow --adopt -d "$DIR" -t "$HOME" macos
fi

stow --adopt -d "$DIR" -t "$HOME" dotfiles
